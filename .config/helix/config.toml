theme="mytheme"

[editor]
line-number="relative"
rulers = [80, 120]

[editor.cursor-shape]
insert = "bar"
normal = "block"
select = "underline"

[keys.normal]
# Vim-Style Undo/Redo (just to not confuse muscle memory to much)
C-r = "redo"
u = "undo"

";" = "repeat_last_motion" # vim-style (move to next, after f,F,t,T)

# these are different keystrokes, having them both on one key saves time 
esc = ["collapse_selection", "keep_primary_selection"]

# Split navi via ctrl-hjkl
C-h = ["jump_view_left"]
C-j = ["jump_view_down"]
C-k = ["jump_view_up"]
C-l = ["jump_view_right"]

[keys.normal.space.b]
# E ZQWA - makes quitting a lot less painfull
b = "buffer_picker"
z = ":quit"           # quit buffer, warn if unsaved
q = ":quit!"          # quit buffer, ignore unsaved
w = ":write-quit-all" # save all and quit all
a = ":quit-all"       # quit all, warn if unsaved
d = ":buffer-close"   # vim => delete buffer
S-d = ":buffer-close!"   # vim => delete buffer

[keys.normal.E]
# E r => make run
"r" = [
  ":new",
  ":insert-output tmux split zsh",
  ":buffer-close!",
  ":redraw"
]

# E E => external Explorer
"e"  =  [
  ":new",
  ":insert-output lf-pick",
  "select_all",
  "split_selection_on_newline",
  "goto_file",
  "goto_last_modified_file",
  ":buffer-close!",
  ":redraw"
]

# E g => LazyGit
"g" = [ ":new", ":insert-output lazygit", ":buffer-close!", ":redraw" ]

 # E d => LazyDocker
"d" = [ ":new", ":insert-output lazydocker", ":buffer-close!", ":redraw" ]

[keys.select]
# Original Upper / Lower conversions are awefull on german QWERTZ
S-u="switch_to_uppercase"
u="switch_to_lowercase"
