#!/usr/bin/env bash

prefix=${PASSWORD_STORE_DIR-~/.password-store}
cmd="find \"$prefix\" -type f -name '*.gpg' | sed -e \"s#${prefix}/##g\" | sed -e \"s/\\.gpg\$//g\" | fzf"
password_file=`eval $cmd`
pass show -c $password_file;
