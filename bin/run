#!/bin/bash

in=`cat /dev/stdin | sed -e 's/\s/\\\\ /g'`

if [[ "$in" != "" ]]; then
	echo "in: "$in
	dir=`dirname "$in"`
	echo "dir: "$dir
	base=`basename "$in"`

    eval "cd $dir && ./$base"
else
    exit 1
fi

