#!/bin/bash

read in

in=`sed -e "s| \|\t|\\\\ |g" <<< $in`

if [[ "$in" != "" ]]; then
	# echo "in: "$in
	dir=`dirname "$in"`
	# echo "dir: "$dir
	base=`basename "$in"`

    eval "cd $dir && ./$base"
else
    exit 1
fi

